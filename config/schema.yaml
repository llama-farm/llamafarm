# yaml-language-server: $schema=http://json-schema.org/draft-07/schema#

$schema: http://json-schema.org/draft-07/schema#
title: LlamaFarm Config
type: object
required:
  - version
  - name
  - namespace
  - runtime
properties:
  version:
    type: string
    enum: ["v1"]
    default: v1
    description: Config version, must be "v1"
  name:
    type: string
    description: Project name
    example: my-project
  namespace:
    type: string
    description: Project namespace
    example: my-namespace
  prompts:
    type: array
    items:
      type: object
      required:
        - content
      properties:
        role:
          type: string
          description: Prompt role (e.g., "system", "user", "assistant")
        content:
          type: string
          description: Prompt content (e.g., "You are a helpful assistant.")
      example:
        role: system
        content: You are a helpful assistant that can answer questions and help with tasks.
    default: []

  rag:
    $ref: "../rag/schema.yaml"

  datasets:
    type: array
    description: List of dataset configurations
    items:
      type: object
      required:
        - name
        - files
        - data_processing_strategy
        - database
      properties:
        name:
          type: string
          description: Dataset name
        data_processing_strategy:
          type: string
          description: RAG data processing strategy to use for the dataset
        database:
          type: string
          description: RAG database to use for the dataset
        files:
          type: array
          description: List of file hashes
          items:
            type: string
            description: sha256 of the file content (e.g., "2b3e321d021e5c625a4a003f0624801fa46faab59b530caddcd65a5c106b8a17")
          default: []
    default: []

  runtime:
    type: object
    properties:
      default_model:
        type: ["string", "null"]
        description: "Name of the default model to use (references a model name in the models list). If not specified, uses first model."

      models:
        type: ["array", "null"]
        description: "List of model configurations for multi-model support"
        items:
          type: object
          required:
            - name
            - provider
            - model
          properties:
            name:
              type: string
              description: "Model identifier (unique name)"
            description:
              type: ["string", "null"]
              description: "Human-readable description of this model configuration"
            provider:
              type: string
              enum: [openai, ollama, lemonade]
              description: "Runtime provider for this model"
            model:
              type: string
              description: "Model name or ID"
            base_url:
              type: ["string", "null"]
              description: "Base URL for the provider"
            api_key:
              type: ["string", "null"]
              description: "API key for the provider"
            instructor_mode:
              type: ["string", "null"]
              description: "Instructor mode to use for structured output (e.g., tools, json, md_json)"
            prompt_format:
              type: ["string", "null"]
              enum: [structured, unstructured, null]
              description: "Prompt formatting pipeline (structured uses Instructor; unstructured sends freeform prompts)"
            model_api_parameters:
              type: ["object", "null"]
              description: "Additional parameters passed to the API provider"
              additionalProperties: true
            lemonade:
              type: ["object", "null"]
              description: "Lemonade-specific configuration"
              properties:
                backend:
                  type: ["string", "null"]
                  enum: [onnx, llamacpp, transformers, null]
                  default: onnx
                  description: "Inference backend for Lemonade"
                port:
                  type: ["integer", "null"]
                  default: 11534
                  description: "Port for Lemonade server"
                context_size:
                  type: ["integer", "null"]
                  default: 32768
                  description: "Context window size in tokens"
                model_path:
                  type: ["string", "null"]
                  description: "Custom model storage location"
                checkpoint:
                  type: ["string", "null"]
                  description: "Full HuggingFace checkpoint path including quantization (e.g., 'bartowski/Llama-3.2-1B-Instruct-GGUF:Q4_K_M')"
                recipe:
                  type: ["string", "null"]
                  enum: [llamacpp, transformers, onnx, null]
                  description: "Lemonade recipe to use with the model (should match backend)"
