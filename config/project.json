{
  "$schema": "https://json.schemastore.org/nx-project",
  "name": "config",
  "root": "config",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/config",
        "command": "uv run pytest -q"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/config",
        "command": "uv run ruff check ."
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/config",
        "command": "uv run mypy config"
      }
    },
    "compile-schema": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/config",
        "command": "uv run python compile_schema.py"
      }
    },
    "generate-types": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/config",
        "command": "uv run python generate_types.py"
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/schema.yaml",
        "{projectRoot}/compile_schema.py",
        "{workspaceRoot}/rag/schema.yaml"
      ],
      "outputs": [
        "{projectRoot}/datamodel.py",
        "{projectRoot}/schema.deref.yaml",
        "{workspaceRoot}/cli/cmd/config/schema.json"
      ]
    }
  }
}