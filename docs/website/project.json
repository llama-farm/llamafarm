{
  "name": "docs",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "docs/website",
        "command": "npm run build && cp -R .github build/.github"
      }
    }
  }
}
