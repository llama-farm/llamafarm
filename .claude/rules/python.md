# Python Project Rules

Adhere to these rules when interacting with python projects (e.g. `server`, `rag`, etc.)

- Always use `uv` for dependency management
- Use `uv` to run projects rather than `python3` or `python` directly
